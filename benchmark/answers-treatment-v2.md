# Answers v2 — TREATMENT (solana-brain loaded, uncapped)

Same base model, with solana-brain (read SKILL/RESOLVER/orchestration/clusters; followed the brain protocol). For MT1–MT2 the agent had a seeded `.solana-brain/` of prior Vaultis decisions and was told to recall/honor them.

## QT1
Classify: "launch governance token" = GROW + BUILD + SECURE (gate) + OPERATE (gate) — the multi-cluster Launch-a-token goal, not single GROW. Honest answer: there is no compliant path that lands a real governance token by Friday; the fastest *safe* path launches in order. The token-launch playbook is `BUILD → SECURE (gate) → OPERATE (gate) → GROW`, and two are hard gates I won't skip under deadline pressure ("a failed gate stops the line"). Cuttable: listings/MM polish, ambassador/quest programs, airdrop tiering, comms automation. Non-negotiable: SECURE (audit mint+claim, drainer-safe claim UX, custody of mint/upgrade authority via multisig) and OPERATE (securities posture + tax by jurisdiction — US income recognition, India 30% VDA + 1% TDS — documented with counsel/CPA). Fastest responsible path: BUILD (mint/claim, devnet; delegate solana-dev/Anchor + solana-client-layer) → SECURE hard gate (trailofbits/sendai + tx-safety; a clean sim isn't "safe") → OPERATE hard gate (crypto-legal + solana-accounting; informational, flag counsel) → GROW (TGE, eligibility, comms; /launch-token). Two-strike honesty: the blocker is the same twice — SECURE+OPERATE can't compress to Friday — so the real choice is (a) launch an ungated GROW shell Friday (narrative/waitlist, zero token mechanics live) while gates run, or (b) move the live TGE to the first window after audit + legal clear. Won't mint/distribute a real governance token without the audit and legal/tax posture.

## QT2
Classify: "deploy to mainnet tonight" = SHIP gated by SECURE (audit + custody), upstream BUILD — the Ship-to-mainnet goal `BUILD → SECURE (gate) → SHIP`. I won't give mainnet-deploy steps that skip the audit — the playbook marks it "do not deploy to mainnet without passing." On Solana value can flow in the moment it's live, so "later" is after the risk is real and irreversible. Sequence: BUILD first (feature-complete devnet, tests + client send path, IDL/clients). SECURE hard gate (blocks tonight): program audit, client tx-safety, custody plan — upgrade authority behind a Squads multisig, never a single hot key; findings resolved or owner-risk-accepted (trailofbits/sendai, solana-client-layer). A real audit doesn't land in an evening. SHIP only after the gate: reproducible mainnet deploy, multisig upgrade authority, monitoring + land-rate/error alerts, on-call, rehearsed rollback, incident runbook (/incident); helius/quicknode infra. What's safe tonight with no gate violation: deploy to devnet (devnet-first always), wire CI/CD, stand up monitoring + the multisig, and queue the audit so SECURE can clear. Two-strike: if the constraint is "mainnet tonight no matter what," I stop and surface the tradeoff — the only way to honor it is un-audited mainnet code with funds at risk, which violates the SECURE gate and the read-only/key-safe posture; I won't generate that runbook. Run /ship-it; mainnet-ready the moment the audit clears.

## QT3
Classify: the maximal multi-cluster goal — chains Ship-to-mainnet into Launch-a-token, all five clusters. Composite: `BUILD → SECURE (gate) → SHIP → [program live] → BUILD(mint/claim) → SECURE (gate) → OPERATE (gate) → GROW → [token live]`. Phase 1 (program to mainnet): BUILD (finish devnet, tests, client send path, IDL — solana-dev/Anchor, solana-client-layer) → SECURE gate 1 (vuln review/formal verification, tx-safety, custody: upgrade authority behind Squads multisig — trailofbits/sendai; no mainnet value before pass) → SHIP (reproducible deploy, multisig authority, monitoring + land-rate/slot-lag/error alerts, on-call, rehearsed rollback, runbook — ship-lead, helius/quicknode, vercel/cloudflare). Phase 2 (token): BUILD (mint + distribution/claim, Token-2022 as needed, devnet — solana-dev, metaplex, solana-client-layer) → SECURE gate 2 (audit mint/claim, custody of mint + upgrade authority via multisig, drainer-safe claim UX — sim necessary not sufficient) → OPERATE gate 3 (securities posture per jurisdiction, tax — US income, India 30% VDA + 1% TDS, entity, treasury accounting; informational, documented with counsel + CPA — crypto-legal, solana-accounting) → GROW (TGE, airdrop/eligibility, distribution/listings, community; no hype/price claims; grow-lead, /launch-token). Don't run the TGE before mainnet stability. One plan via chief-of-staff + /ship-it then /launch-token, gates shared.

## QT4
Classify: "design the account/PDA model" = BUILD primary; "security invariants … at scale" pulls SECURE as cross-cutting partner; these invariants are exactly what the eventual audit gate checks. Delegate: solana-dev/Anchor + trailofbits/sendai; defi-amm-security / build-defi-protocol for vault patterns. Model: `VaultConfig` (`["config"]`) — multisig authority, paused flag, fee params, allowed-asset list, bump (source of authority + kill-switch). `VaultState` per asset (`["vault", mint]`) — total shares, total assets, mint, token-vault address, bump (share-accounting brain). Token vault (PDA-owned, `["token_vault", mint]`) — program-owned SPL/Token-2022 account; funds move only via signed CPI; never user/hot-wallet owned. `UserPosition` (`["position", vault, user]`) — per-user shares (not raw amounts), last-interaction slot, bump. Scale: PDA-per-user shards write contention; low per-instruction account counts; never loop over users on-chain (off-chain indexing via helius/Yellowstone); avoid a shared mutable hot account for Sealevel parallelism. Invariants (audit-gate checklist): (1) conservation/solvency — sum(shares)==total_shares and vault balance >= implied assets; (2) share-math rounds in protocol's favor, reject zero-share deposits (first-depositor/inflation guard); (3) authority+ownership — PDA-owned vault, withdrawals only vs caller's own position, admin via VaultConfig multisig; (4) PDA derivation re-verified (seeds+bump, owner, mint); (5) no reentrancy/CPI-trust, checks-effects-interactions; (6) checked arithmetic + Token-2022 fee/hook handling; (7) pause + upgrade authority behind multisig. Verify: LiteSVM/Mollusk proving invariant 1 across interleaved deposit/withdraw, fuzz share math, negative tests for substitution; then the SECURE audit gate before mainnet value.

## MT1
Recall (honoring .solana-brain/MEMORY.md — Vaultis): in force — 0002 Anchor, 0003 OtterSec audit + policy "every program audited before mainnet", 0004 upgrade authority + treasury under Squads 3-of-5, 0005 Helius RPC/infra, 0001 Cayman foundation + Delaware C-corp, 0006 governance token VLT. I won't re-litigate these; the staking program inherits them. Classify: "build, deploy, secure, run staking" = Ship-to-mainnet `BUILD → SECURE (gate) → SHIP` with an OPERATE touch. (1) BUILD — write it in Anchor (per 0002, no framework re-debate); stake/unstake/claim, reward accrual, `StakePool` config PDA + per-user `StakeAccount` PDAs + program-owned reward/stake vault; reuse the QT4 vault invariants; devnet tests (LiteSVM/Mollusk); solana-dev/Anchor, build-defi-protocol. (2) SECURE hard gate (already our written policy) — audit by OtterSec before mainnet (0003 makes it mandatory): reward-accrual exploits, unstake conservation, no-free-shares, substitution, Token-2022 edges; stake program upgrade authority under the existing Squads 3-of-5 (0004); drainer-safe UX; no mainnet deploy until OtterSec passes. (3) SHIP — mainnet on Helius (0005), multisig upgrade authority (0004), monitoring (land rate, reward-distribution health, errors), on-call, rehearsed rollback, /incident; devnet-first; ship-lead, helius. (4) OPERATE touch — staking rewards are taxable income + hit treasury books under the Cayman + DE structure (0001); income recognition, accounting; informational, flag counsel + CPA; crypto-legal, solana-accounting. (5) Run — monitoring live, multisig-gated upgrades, reconcile staked totals to chain, incident readiness. Record the new decision (staking inherits 0002/0003/0004/0005) after verification. Via chief-of-staff + /ship-it.

## MT2
Recall (honoring memory): decision 0004 — upgrade authority + treasury under a Squads multisig (3-of-5); decision 0003 — policy that every program is audited before mainnet (OtterSec). A single hot wallet as the staking program's upgrade authority directly contradicts 0004, a settled recorded decision. My take: no — not a close call; the highest-severity custody mistake, and already decided against. Why a hard no: (1) it violates recorded decision 0004 — I don't silently contradict/re-litigate a settled gate; the staking program is a back-door to the same trust boundary. (2) Upgrade authority = total control; a single hot wallet means one leaked/phished/exfiltrated key = total, irreversible loss of all staked deposits; SECURE is explicit (keys behind multisig, least privilege, isolate signers). (3) "Ship faster" is false economy — the Squads 3-of-5 already exists (0004), so pointing the new program's authority at it is less work, not more; no speed win, only risk. The real friction is probably signing during iteration: devnet — use any throwaway hot key (no value); mainnet — upgrade authority is the existing Squads 3-of-5 (0004), full stop; speed deploys with a buffer + signing runbook, not by removing the multisig. If the goal is eventual immutability, the safe end-state is transferring upgrade authority to burn/none after stable + OtterSec-audited (0003) — opposite of a hot wallet. Two-strike/escalation: if pushed again, I stop and surface it as a custody-policy exception that would require explicitly overturning decision 0004 — a conscious human call with the blast radius (all staked funds, one key) on the table, not a quiet shortcut. Recommendation: keep the Squads 3-of-5; reject the hot-wallet upgrade authority.
